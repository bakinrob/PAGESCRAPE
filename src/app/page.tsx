import { FordScraperApp } from "@/components/ford-scraper-app";

export const metadata = {
  title: "Dealer Page Migration Workspace",
  description: "OEM-agnostic dealer static page migration with destination package-driven rebuilds.",
};

export default function Home() {
  return <FordScraperApp />;
}
